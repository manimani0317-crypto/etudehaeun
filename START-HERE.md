# 여기부터 읽으세요 (AI / 새 작업자용)

> 이 파일은 **처음 이 저장소를 보는 Claude·GPT·사람**을 위한 진입점입니다.
> 작업 시작 전에 이 파일부터 읽고, 아래 순서대로 움직이세요.
> 최종 갱신: 2026-07-31

---

## 1. 이 프로젝트는 뭔가요

에뛰드(ETUDE) 화장품 브랜드의 **메인페이지 한 장**을 Figma 시안 그대로 구현한 정적 사이트입니다.
쇼핑몰 기능은 없습니다. **보이는 것만** 만듭니다.

| | |
| --- | --- |
| 결과물 | `static/` 폴더 하나 — HTML 1개, CSS 1개, JS 1개, 이미지 88개 |
| 기술 | **순수 HTML / CSS / JS.** 프레임워크·빌드도구·npm **전부 없음** |
| 배포 | GitHub Pages (`main` 푸시 → 자동) |
| 저장소 | https://github.com/manimani0317-crypto/etudehaeun |
| 화면 | 데스크톱 1920 × 10427px / 태블릿 768 × 9841px — **이 두 벌뿐** |

---

## 2. AI 에게 — 작업 시작 전 필수 3단계

### ① `작업-인계-문서.md` 를 먼저 읽으세요

**이게 이 프로젝트의 진짜 매뉴얼입니다.** 다음이 들어 있습니다.

- 하지 말 것 8가지 (아래 4번에 요약)
- **반드시 알아야 할 함정 7가지** ← 모르면 헛수고합니다
- 섹션별 완료 작업 (좌표·색상·수치까지)
- 피그마 노드 ID 목록
- 검증용 JS 스니펫

이 파일(START-HERE)은 **입구일 뿐**입니다. 실제 작업 정보는 전부 인계 문서에 있습니다.

### ② 최신 코드인지 확인하세요

여러 도구(Claude / Codex)로 **같은 저장소를 병행 작업**합니다. HEAD 가 하루에도 여러 번 바뀝니다.

```bash
git fetch && git log --oneline -5
```

**오래된 베이스에서 고치면 최신 작업을 덮어씁니다.**
로컬 HEAD 가 `origin/main` 과 같은지 반드시 확인하고 시작하세요.

### ③ 손대기 전에 현재 코드 상태를 직접 확인하세요

디자인이 계속 바뀌는 중이라, 문서와 코드가 어긋나 있을 수 있습니다.
**문서보다 코드가 우선**입니다. 추정하지 말고 파일을 여세요.

---

## 3. 집 PC 에서 처음 시작할 때

### 저장소 받기

```bash
git clone https://github.com/manimani0317-crypto/etudehaeun.git
```

### 미리보기 서버 띄우기 — ⚠️ 중요

**`.claude/launch.json` 은 회사 PC(EZEN) 전용입니다.** 그 안의 `Serve-Static.ps1` 경로는
다른 PC 에 존재하지 않으므로 `preview_start` 가 실패합니다. 집에서는 아래 중 하나를 쓰세요.

| 방법 | 명령 | 비고 |
| --- | --- | --- |
| **VS Code Live Server** (권장) | `static/index.html` 우클릭 → Open with Live Server | 가장 간단 |
| node 가 있다면 | `npx serve static` | Range 지원 O |
| ~~`python -m http.server`~~ | — | **쓰지 마세요.** Range 미지원 + 단일 스레드라 영상 7개가 물리면 멈춥니다 |

> **`file://` 로 직접 열지 마세요.** JS 가 캐시돼서 수정이 반영되지 않고,
> Instagram 영상(mp4)은 Range 요청이 필요해 재생이 깨집니다.

서버를 직접 만들어야 한다면 인계 문서 0번의 **4가지 조건**(멀티스레드 / Range 지원 /
독립 프로세스 / `Cache-Control: no-store`)을 반드시 반영하세요. 전부 실제로 겪은 문제입니다.

---

## 4. 🚫 하지 말 것 — 요약

전부 **의도된 설계**입니다. 개선하려다 망가뜨리는 일이 반복돼서 못 박아 둡니다.
(이유와 상세는 인계 문서 0번)

| 하지 말 것 | 한 줄 이유 |
| --- | --- |
| 프레임워크·빌드도구 도입 (React, Tailwind, npm …) | 순수 HTML/CSS/JS 가 이 프로젝트의 전제. 2026-07-31 에 Next.js 를 걷어냈습니다 |
| **"반응형으로 개선"** | 1920px 고정 캔버스를 `zoom` 으로 스케일하는 게 **의도된 방식**입니다 |
| CSS 일괄 치환 | 하위 요소의 `calc(1 / 2 * 100%)` 는 정상입니다 |
| 시안에 없는 요소 추가 | 없는 건 "없음"이 정답. 추정 금지 |
| 이미지 리사이즈·재생성 | export 자산은 이미 잘린 결과물이라 늘리면 찌그러집니다 |
| `static/` 밖 파일 수정 | 나머지는 문서·설정·디자이너 원본입니다 |
| 클래스명 리팩터링 | 클래스명 = Figma 레이어명(`data-name`), 1:1 대응입니다 |

---

## 5. 지금 상태 (2026-07-31)

### 저장소 구조

```
static/                 ← 작업 대상은 여기뿐
  index.html              페이지 전체 마크업
  css/style.css           모든 스타일
  js/main.js              매장찾기 · GNB/LNB · Instagram
  images/main/            PNG 65 · SVG 15 · MP4 8

assets/                 디자이너 원본 Figma 에셋 101개 (여기서 골라 복사해 씁니다)
.github/workflows/      GitHub Pages 배포

작업-인계-문서.md        ← 진짜 매뉴얼
README.md               개요 · 실행 · 배포
static/README.md        코드 구조와 수정 방법
```

### 남은 일

1. 🔴 **GitHub Pages Source 설정 미변경** — 저장소 소유자만 가능합니다
   - Settings → Pages → Source 를 `Deploy from a branch` → **`GitHub Actions`** 로
   - 그전까지 루트 URL 은 README 가 렌더링됩니다. 실물은
     https://manimani0317-crypto.github.io/etudehaeun/static/
   - **코드로 고치려 하지 마세요.** 저장소 설정 문제입니다
2. Instagram 휘는 정도 미세조정 (`RADIUS` 1100 / `STEP_DEG` 20)
3. `Insta_Lilus_08.mp4` 미사용 (카드가 7장이라 8번은 안 쓰임)
4. 미사용 CSS 잔존 — 목록은 인계 문서 4번. 무해하니 요청받았을 때만 정리

---

## 6. 사용자용 — 집에서 이렇게 시작하세요

새 대화를 열고 아래를 그대로 붙여넣으면 됩니다.

```
이 저장소의 START-HERE.md 와 작업-인계-문서.md 를 먼저 읽고,
현재 코드 상태를 확인한 다음 작업을 시작해줘.
프레임워크 도입이나 반응형 개조는 하지 말고, static/ 폴더만 건드려줘.
```

작업이 끝나면 인계 문서의 **8번 「변경 이력」** 에 무엇을 왜 바꿨는지 덧붙여 달라고 하세요.
그래야 다음 세션이 또 처음부터 헤매지 않습니다.
