# Claude 대화 백업 — 2026-07-30

> 프로젝트: `C:\0723김하은\김하은\etudehaeun`
> 이 문서는 2026-07-30 Claude 세션에서 실제로 한 작업과, 그 과정에서 알아낸 함정들을 정리한 것입니다.
> 기존 `작업-인계-문서.md`(07:43 작업분)는 수정하지 않았습니다. 두 문서를 함께 보면 됩니다.

---

## 0. 이 세션에서 한 일 요약

1. GitHub에서 최신 파일 받아오기 (로컬과 갈라져 있던 상태 정리)
2. **1920px 기준 화면 폭 100%** 작업 복원 — 전날 작업이 GitHub 최신본에 빠져 있었음
3. **Instagram 슬라이더를 원통(cylinder) 배치로 변경**
4. **미리보기 서버 문제 해결** — 이 PC에는 node가 없어서 인계 문서의 서버를 못 씀

---

## 1. GitHub 동기화 (오전)

### 상황

로컬과 원격이 양쪽으로 갈라져 있었습니다.

| | 커밋 |
| --- | --- |
| 로컬만 | `a53b421 0729PM20209`, `093ad1a pm0956` |
| 원격만 | `f29d475 0729`, `62c67bf 디자인 반영…` |

병합 예행연습(`git merge-tree`) 결과 충돌은 6개 파일에서만 발생할 예정이었지만,
사용자가 "GitHub 파일 다 가져와줘"를 선택해서 원격 상태로 맞췄습니다.

### 처리

```bash
git branch backup/local-pm0956-0729 093ad1a   # 로컬 작업 백업
git reset --hard origin/main
```

**로컬 작업은 지우지 않고 `backup/local-pm0956-0729` 브랜치에 보관돼 있습니다.**
되돌릴 일이 있으면 `git reset --hard backup/local-pm0956-0729`.

`origin/design/0730-lnb-since-instagram` 브랜치도 로컬에 만들어 뒀는데,
**`origin/main`과 같은 커밋**이라 별도 작업은 없습니다.

### ⚠️ 이 세션 중에 저장소가 계속 움직였습니다

같은 폴더에서 Codex 작업이 병행돼서, HEAD가 세션 도중 여러 번 바뀌었습니다.

```
62c67bf (07:43) → c1baf69 (13:57) → 9726377 (21:17)
```

**작업 시작 전에 반드시 `git fetch` + `git log`로 현재 HEAD를 확인하세요.**
오래된 베이스에서 고치면 최신 작업을 덮어씁니다.

---

## 2. 폭 100% (풀화면) 복원

### 문제

최상위 섹션 14개 중 `.메인-footer` / `.순정` / `.이벤트` 3개만 가운데 정렬이라
**1920px을 넘는 화면에서 이 3개만 오른쪽으로 밀렸습니다.**

전날 로컬(`pm0956`)에서 고쳤지만 그 작업이 GitHub 최신본에 반영되지 않은 상태였습니다.

### 확인해보니 3가지 중 2개는 이미 들어와 있었음

| 항목 | 상태 |
| --- | --- |
| `body { container-type: inline-size }` | 이미 있음 |
| `main.js` 의 `pageZoom()` 드래그 보정 | 이미 있음 |
| `.page` 의 `zoom` 이 `100cqw` | **빠져 있었음** (`100vw` 로 되어 있었음) |
| 섹션 3개 `left: 0` | **빠져 있었음** |

### 수정 (`static/css/style.css`)

```css
.page {
  width: 1920px;
  /* 100vw 는 세로 스크롤바 폭이 포함돼 가로 스크롤이 1px 생깁니다.
     body 의 container-type: inline-size 덕분에 100cqw 는 스크롤바를 뺀 폭이라 딱 맞습니다. */
  zoom: tan(atan2(100cqw, 1920px));
}

/* .메인-footer / .순정 / .이벤트 — 셋 다 아래처럼 */
.순정 {
  left: 0px;        /* calc(1 / 2 * 100%) 였음 */
  translate: none;  /* --tw-translate-x 선언과 함께 제거 */
}
```

`.gmb` 는 이미 `left: 0px; translate: none;` 이라 건드리지 않았습니다.

### 태블릿 안전 확인

미디어쿼리(`max-width: 1024px`)가 이 세 섹션을 **이미 `left: 0; translate: none;` 으로 재정의**하고 있어서,
베이스 규칙을 바꿔도 태블릿 렌더링에는 영향이 없습니다. (수정 전에 확인함)

### 검증 결과

| 뷰포트 | zoom | 렌더 폭 | 가로 스크롤 | 최상위 섹션 |
| --- | --- | --- | --- | --- |
| 1920 | 0.992 | 1905 (꽉 참) | 0 | 8/8 `left: 0` |
| 2560 | 1.326 | 2545 (꽉 참) | 0 | 8/8 `left: 0` |

깨진 이미지 0장, CSS 중괄호 균형 0.

> **주의**: `calc(1 / 2 * 100%)` 가 CSS 에 아직 49곳 남아 있는데 **정상입니다.**
> `.클로버`, `.new_strawberry_icon01-2` 같은 **하위 요소**들이고, 1920px 부모 안에서
> 가운데 정렬되는 게 맞습니다. 최상위 섹션만 `left: 0` 이어야 합니다.

---

## 3. Instagram 슬라이더 — 원통(cylinder) 배치

### 요청

"카드가 양옆으로 빨려가듯 휘어지게" (사용자가 손그림 스케치로 전달)

처음에 "쫙 펼쳐진"을 **평평하게**로 잘못 해석해서 회전을 0으로 만들었다가,
"너무 일렬"이라는 피드백을 받고 다시 작업했습니다.

### 수정 (`static/js/main.js`)

카드를 **원통 위의 좌표**에 올립니다. 각 카드가 원의 접선 방향을 보게 해서
옆으로 갈수록 자연스럽게 말려 들어가고 뒤로 물러납니다.

```js
var RADIUS   = 1100;  // 원통 반지름 — 클수록 완만하게 휘고 좌우로 더 넓게 퍼짐
var STEP_DEG = 20;    // 카드 한 칸당 각도 — 클수록 급하게 휨

// render() 안 (데스크톱 분기)
var ang = off * STEP_DEG * Math.PI / 180;   // off 는 부호 있고 드래그 중엔 소수
x    = RADIUS * Math.sin(ang);
z    = RADIUS * (Math.cos(ang) - 1);        // 가운데 0, 옆으로 갈수록 뒤로
rotY = -off * STEP_DEG;                     // 카드가 항상 원통 바깥을 향하도록
```

### 결과 좌표

| 위치 | 가운데 | ±1 | ±2 | ±3 |
| --- | --- | --- | --- | --- |
| x | 0 | ±376 | ±707 | ±953 |
| z (깊이) | 0 | −66 | −257 | −550 |
| 회전 | 0° | ∓20° | ∓40° | ∓60° |
| 배율 | 1.2 | 1.0 | 1.0 | 1.0 |

- 드래그 중에도 각도가 소수로 계산돼 링 전체가 손가락을 따라 매끄럽게 돌아갑니다.
- **태블릿(≤1024px)은 기존 평평한 시안 그대로** 두었습니다 (`tabletInsta` 분기).
- 조절은 `RADIUS` / `STEP_DEG` 두 숫자만 만지면 됩니다.
  더 급하게 말리게 → `STEP_DEG` 25~28 / 더 넓게 퍼뜨리기 → `RADIUS` 1300~1500

---

## 4. 미리보기 서버 — 이 PC에는 node 가 없습니다

### 문제

인계 문서의 `.claude/launch.json` 은 **다른 PC 경로**(`C:\Users\haeun\...`)를 가리키고 있었고,
이 PC(EZEN)에는 **node 가 아예 설치되어 있지 않습니다.** `python` 도 Windows Store 스텁이라 동작 안 함.

### 해결 — PowerShell 정적 서버

`launch.json` 의 `etude-static` 을 PowerShell 스크립트로 교체했습니다.
스크립트 위치(스크래치패드라 세션이 바뀌면 사라질 수 있음):

```
C:\Users\EZEN\AppData\Local\Temp\claude\C--0723--------etudehaeun\
  04eb4d78-92b9-4d4b-8b4f-57130e2172bb\scratchpad\Serve-Static.ps1
```

직접 띄우려면:

```
powershell -ExecutionPolicy Bypass -NoProfile -File <위 경로> -Root "C:\0723김하은\김하은\etudehaeun\static" -Port 3100
```

### 여기서 걸린 함정 3가지 (다시 만들 때 꼭 반영할 것)

1. **단일 스레드로 만들면 멈춥니다.**
   Instagram 영상 7개가 동시에 연결을 붙잡으면 그다음 요청이 전부 대기에 걸립니다.
   → 워커 런스페이스 여러 개(8개)가 같은 `HttpListener` 를 나눠 처리하도록 해야 합니다.

2. **Range 요청을 지원해야 합니다.** 영상 탐색에 필요합니다. (`206` + `Content-Range`)

3. **`preview_start` 로 띄운 서버는 세션이 끝나면 같이 죽습니다.**
   사용자가 계속 "안 보여"라고 한 원인이 이것이었습니다.
   → `Start-Process ... -WindowStyle Hidden` 으로 **독립 프로세스**로 띄워야 살아남습니다.

4. `Cache-Control: no-store` 를 붙여야 편집 내용이 바로 반영됩니다.
   (`file://` 로 열면 JS가 캐시돼서 수정이 반영 안 됩니다 — 서버로 보는 게 맞습니다.)

---

## 5. 환경 메모 (이 PC 기준)

- **node 없음, python 없음(Store 스텁), ffmpeg 없음** — 스크립트는 PowerShell 로 작성해야 합니다
- PowerShell 5.1 — `&&`, `||`, 삼항연산자 사용 불가
- **mp4 영상은 Claude 가 열어볼 수 없습니다.** 레퍼런스는 캡처 이미지나 스케치로 주셔야 합니다
  (이번에도 영상 대신 손그림 스케치로 해결했습니다)
- 브라우저 패널이 화면에 표시돼 있지 않으면 스크린샷이 안 찍히거나 갱신되지 않습니다
  → 그럴 땐 `javascript_tool` 로 수치를 재서 검증하는 게 확실합니다

---

## 6. 남은 일 / 확인 필요

1. **Instagram 휘는 정도 확인** — 스케치와 비교해서 `RADIUS` / `STEP_DEG` 미세조정
2. `.claude/launch.json` 이 **이 PC 전용 경로**로 바뀌어 있습니다.
   다른 PC에서 쓰려면 그 PC 경로로 다시 고쳐야 합니다
3. `backup/local-pm0956-0729` 브랜치는 전날 로컬 작업 보관용입니다.
   더 이상 필요 없으면 지워도 됩니다

---

## 7. 참고 — 브라우저에서 바로 검증하는 스니펫

```js
// 폭이 꽉 찼는지 + 섹션 정렬
(() => {
  const page = document.querySelector('.page');
  return {
    zoom: getComputedStyle(page).zoom,
    renderedWidth: Math.round(page.getBoundingClientRect().width),
    hScroll: document.documentElement.scrollWidth - document.documentElement.clientWidth,
    allAtZero: ['.순정','.이벤트','.메인-footer','.메이크업','.new','.매장찾기','.메인슬라이더','.gmb']
      .every(s => Math.round(document.querySelector(s).getBoundingClientRect().left) === 0),
  };
})()

// Instagram 카드 좌표
[...document.querySelectorAll('[data-insta-card]')].map(c => c.style.transform)

// 깨진 이미지
[...document.querySelectorAll('img')].filter(i => i.complete && i.naturalWidth === 0).map(i => i.src)
```
