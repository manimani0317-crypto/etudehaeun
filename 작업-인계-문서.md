# ETUDE 메인페이지 — 작업 인계 문서

> **최종 갱신: 2026-07-31** (Claude 세션)
> 이 파일 하나만 읽고도 작업을 이어갈 수 있도록 정리했습니다.
> **새 대화를 시작할 때 이 파일을 먼저 읽어달라고 하세요.**

---

## 이 프로젝트가 뭔가요 (처음 보는 사람 / AI 용)

에뛰드(ETUDE) 화장품 브랜드의 **메인페이지 한 장**을 Figma 시안 그대로 구현한 정적 사이트입니다.
쇼핑몰 기능은 없습니다. 보이는 것만 만듭니다.

| | |
| --- | --- |
| 결과물 | `static/` 폴더 하나 — HTML 1개, CSS 1개, JS 1개, 이미지 88개 |
| 기술 | **순수 HTML / CSS / JS.** 프레임워크·빌드도구·npm 전부 없음 |
| 배포 | GitHub Pages (`main` 푸시 → 자동) |
| 원본 | Figma 파일 `oN5K5v6JyNKEB8Qp44Bfs7` (아래 6번에 노드 ID 목록) |
| 화면 | 데스크톱 1920 × 10427px / 태블릿 768 × 9841px, 두 벌뿐 |

### 읽는 순서

1. **아래 0번 「기본 규칙」과 「하지 말 것」** ← 여기서 사고가 제일 많이 납니다
2. **1번 「반드시 알아야 할 함정」** ← 모르면 헛수고합니다
3. 고칠 섹션에 해당하는 3번 항목
4. 다 고쳤으면 5번 「검증 방법」

---

## 0. 이 프로젝트를 다룰 때의 기본 규칙

- **작업 대상은 `static/` 폴더뿐입니다.** 순수 HTML / CSS / JS 이고 프레임워크를 쓰지 않습니다.
  - `static/index.html` (1,842줄)
  - `static/css/style.css` (12,484줄)
  - `static/js/main.js` (481줄)
- **Next.js 버전(`src/`)은 2026-07-31 에 제거했습니다.** `nextjs-last` 태그에 남아 있습니다.
  이 저장소는 더 이상 npm 프로젝트가 아닙니다 — `package.json` 도 `node_modules` 도 없습니다.
- `assets/` 는 디자이너가 넣어주는 원본 폴더(101개). 쓸 이미지는 `static/images/main/`(88개) 으로 복사해서 씁니다.
- 설명은 한국어로. 주석도 한국어로 답니다.
- 디자인은 계속 수정되는 중이라, 손대기 전에 **현재 코드 상태부터 확인**하고 시작합니다.

### 🚫 하지 말 것 — "좋은 뜻으로" 하면 더 위험한 것들

여기 적힌 건 전부 **의도된 설계**입니다. 개선하려다 망가뜨리는 일이 반복돼서 못 박아 둡니다.

| 하지 말 것 | 이유 |
| --- | --- |
| **빌드도구·프레임워크 도입** (React, Vue, Tailwind, Vite, npm …) | 순수 HTML/CSS/JS 가 이 프로젝트의 전제입니다. 2026-07-31 에 Next.js 버전을 걷어냈습니다 |
| **"반응형으로 개선"** — 미디어쿼리 추가, `%`·`rem` 로 일괄 변환 | 1920px 고정 캔버스를 `zoom` 으로 스케일하는 방식이 **의도된 설계**입니다. 분기는 태블릿 하나뿐입니다 |
| **CSS 일괄 치환** (`calc(1 / 2 * 100%)` → `left: 0` 등) | 하위 요소는 그 값이 정상입니다. 최상위 섹션만 `left: 0` 이어야 합니다 (함정 ④) |
| **Figma 시안에 없는 요소 추가** | 없는 건 "없음"이 정답입니다. 추정해서 채우지 마세요 |
| **이미지 임의 리사이즈·재생성** | export 자산은 이미 잘린 결과물입니다. 늘리면 찌그러집니다 (함정 ③) |
| **`static/` 밖 파일 수정** | 나머지는 문서·설정·디자이너 원본입니다. 요청 없이 건드리지 마세요 |
| **클래스명 리팩터링** | 클래스명은 Figma 레이어명(`data-name`)과 1:1 입니다. 바꾸면 시안 대조가 불가능해집니다 |
| **미사용 CSS 일괄 삭제** | 목록은 4번에 있습니다. 렌더링에 무해하니 요청받았을 때만 지우세요 |

### ⚠️ 작업 시작 전에 반드시 `git fetch`

이 저장소는 여러 도구(Claude / Codex)에서 같은 폴더로 병행 작업하고 있어서
**HEAD 가 하루에도 여러 번 바뀝니다.** 2026-07-30 하루 동안만 해도:

```
f29d475 (07-29 02:28)
  → 62c67bf (07-30 07:43)  디자인 반영: GNB/LNB, SINCE·Instagram, 폰트
  → 2329cd3 (11:49)        태블릿 레이아웃 및 인스타그램 영상 적용
  → 61562f2 (12:08)        태블릿 레이아웃 디테일 보정
  → 9f396d5 / 4c2531e      GitHub Pages 배포 설정·재배포
  → 9c1a55c (12:59)        메이크업룩 아이템 박스 수정본 복구
  → 8539a94 / 590ae6a      GNB 중복 로고 제거 / 검색 아이콘 고정
  → c1baf69 (13:57)
  → 9726377 (21:17)        폭 100% 복원 + Instagram 원통 배치
  → ad66ac8 (22:08)        문서 정리

07-31
  → a3bf67e (19:24)        Next.js 버전 제거 — static/ 단일 구조로 정리
  → 0bf4f3a (19:28)        빈 guidelines/ 제거          ← 현재 HEAD
```

**오래된 베이스에서 고치면 최신 작업을 덮어씁니다.** 항상 `git fetch` 후 `git log --oneline -5` 로 확인하세요.

참고 태그: `nextjs-last` (= `62c67bf`) — 삭제된 Next.js 버전의 마지막 상태

### 미리보기 실행 — 이 PC(EZEN)에는 node 가 없습니다

`.claude/launch.json` 에는 **`etude-static` 하나만** 있습니다 — PowerShell 정적 서버, 포트 3100.
node·python·ffmpeg 전부 없는 환경이라 node 서버를 쓸 수 없습니다.
(`npm run dev` 를 쓰던 `etude-dev` 설정은 Next.js 제거와 함께 2026-07-31 에 지웠습니다.)

> ⚠️ `launch.json` 이 가리키는 `Serve-Static.ps1` 은 **옛 세션의 스크래치패드 경로**에 있습니다.
> 그 폴더가 지워지면 `preview_start` 가 실패합니다. 그때는 아래 4가지 조건대로 새로 만드세요.

```
powershell -ExecutionPolicy Bypass -NoProfile -File <스크래치패드>\Serve-Static.ps1 -Root "C:\0723김하은\김하은\etudehaeun\static" -Port 3100
```

**서버를 새로 만들어야 한다면 아래 4가지를 꼭 반영하세요** (전부 실제로 겪은 문제입니다):

1. **멀티스레드여야 합니다.** Instagram 영상 7개가 동시에 연결을 붙잡으면 단일 스레드 서버는 멈춥니다.
   → 워커 런스페이스 8개가 같은 `HttpListener` 를 나눠 처리
2. **Range 요청(`206` + `Content-Range`) 지원** — 영상 탐색에 필요
3. **독립 프로세스로 띄울 것** — `preview_start` 로 띄운 서버는 세션이 끝나면 같이 죽습니다.
   `Start-Process powershell ... -WindowStyle Hidden` 사용
4. **`Cache-Control: no-store`** — 안 붙이면 수정이 반영 안 됩니다

> `file://` 로 직접 열면 JS 가 캐시돼서 수정이 반영되지 않습니다. 반드시 서버로 보세요.

---

## 1. 반드시 알아야 할 함정

이걸 모르면 헛수고합니다.

### ① `[data-name="매장찾기"]` 는 **숨겨져 있습니다**
`style.css` 에 `[data-name="매장찾기"] { visibility: hidden !important; }` 가 있습니다.
피그마 export 원본 블록이고, **실제로 보이는 건 `[data-offline-section]`(`.div-20`) 인터랙티브 버전**입니다.
매장찾기를 고칠 땐 `.div-20 ~ .div-56` 쪽을 고쳐야 합니다.

### ② `.gmb` 안에 엉뚱한 게 들어있었습니다
export 구조상 `.gmb`(GNB) 블록 안에 Since 섹션 조각들이 자식으로 들어가 있었습니다.
지금은 정리해서 `.gmb > .gmb-inner > (.gmb-div, .gnb-lnb)` 만 남았습니다.
**DOM 을 추가할 땐 항상 부모가 어디인지 먼저 확인하세요.**

### ③ 피그마 export 이미지는 **이미 잘린 결과물**입니다
`get_design_context` 가 주는 중첩 래퍼 + 퍼센트 좌표를 그대로 믿고 늘리면 **이미지가 찌그러집니다.**
- `Offline_map.png`(921×610) = 지도 원본 `맵.png`(1284×1225) 를 잘라낸 결과물
- 드래그가 필요한 지도는 **원본** `Offline_map_full.png`(=`맵.png`) 를 써야 함
- 반대로 `ev_img.png`(609×827) 는 잘린 결과물을 1:1 로 쓰는 게 정답

**판별법**: export 자산의 실측 크기가 컨테이너 크기와 똑같으면 "이미 잘린 것"입니다.

### ④ `calc(1 / 2 * 100%)` 가 49곳 남아 있는데 **정상입니다**
`.클로버`, `.new_strawberry_icon01-2` 같은 **하위 요소**들이고 1920px 부모 안에서 가운데 정렬되는 게 맞습니다.
**최상위 섹션만 `left: 0`** 이어야 합니다. (아래 3-1 참고) 일괄 치환하지 마세요.

### ⑤ 피그마 MCP 는 간헐적으로 실패합니다
`get_design_context` / `get_metadata` 가 `Invalid JSON: EOF while parsing a string` 로 죽을 때가 있습니다.
**재시도하면 됩니다.** (`get_screenshot` 은 거의 항상 성공 / `get_metadata` 는 이 파일에서 계속 실패)

### ⑥ 브라우저 도구가 자주 막힙니다
`classifier temporarily unavailable` 로 미리보기 조작이 안 될 때가 있습니다.
브라우저 패널이 화면에 표시돼 있지 않으면 **스크린샷이 안 찍히거나 갱신되지 않습니다.**
→ 그럴 땐 `javascript_tool` 로 수치를 재서 검증하는 게 확실합니다 (아래 5번 스니펫).

### ⑦ mp4 영상은 AI 가 열어볼 수 없습니다
레퍼런스는 **캡처 이미지나 스케치**로 주셔야 합니다. (ffmpeg 도 없어서 프레임 추출 불가)

---

## 2. 반응형 구조

미디어쿼리는 딱 2개입니다.

| 분기 | 용도 |
| --- | --- |
| `@media (max-width: 1024px)` | 태블릿 레이아웃 전체 (768px 기준, `zoom: tan(atan2(100vw, 768px))`) |
| `@media (width: 768px)` | 정확히 768px 일 때 `zoom: 1` (Chromium 합성 문제 대응) |

**태블릿 분기가 데스크톱 베이스 규칙을 재정의하고 있습니다.**
베이스를 고칠 땐 태블릿 쪽에 같은 선택자가 있는지 먼저 확인하세요.

태블릿 섹션 위치 (Figma `8580:4925`, 768 × 9841px 기준):

| 섹션 | Y | 높이 |
| --- | ---: | ---: |
| Hero | 0 | 755 |
| Makeup Look | 862 | 1405 |
| NEW | 2367 | 926 |
| SoonJung | 3404 | 853 |
| Offline Store | 4533 | 970 |
| Event | 5582 | 1080 |
| Instagram | 6680 | 989 |
| Since | 7861 | 1396 |
| Footer | 9255 | 586 |

---

## 3. 완료된 작업

### 3-1. 폭 100% (풀화면) — 2026-07-30 21:17

최상위 섹션 중 `.메인-footer` / `.순정` / `.이벤트` 3개만 가운데 정렬이라
**1920px 을 넘는 화면에서 이 3개만 오른쪽으로 밀렸습니다.**

```css
body { container-type: inline-size; }

.page {
  width: 1920px;
  /* 100vw 는 세로 스크롤바 폭이 포함돼 가로 스크롤이 1px 생깁니다.
     container-type 덕분에 100cqw 는 스크롤바를 뺀 폭이라 딱 맞습니다. */
  zoom: tan(atan2(100cqw, 1920px));
  overflow-x: clip;
  overflow-y: visible;
}

/* .메인-footer / .순정 / .이벤트 — 셋 다 */
left: 0px;        /* calc(1 / 2 * 100%) 였음 */
translate: none;  /* --tw-translate-x 선언과 함께 제거 */
```

- `zoom` 은 숫자만 받는데 `calc(100cqw / 1920px)` 이 안 돼서 `tan(atan2(a,b))` 로 비율을 만듭니다.
- `main.js` 의 `pageZoom()` 이 지도 드래그 시 화면 px ↔ 캔버스 px 단위를 보정합니다.
- **원본 1920px 크기로 되돌리려면 `zoom` 한 줄만 지우면 됩니다.**

검증:

| 뷰포트 | zoom | 렌더 폭 | 가로 스크롤 | 최상위 섹션 |
| --- | --- | --- | --- | --- |
| 1920 | 0.992 | 1905 (꽉 참) | 0 | 8/8 `left: 0` |
| 2560 | 1.326 | 2545 (꽉 참) | 0 | 8/8 `left: 0` |

### 3-2. 폰트 (전역)

1. **깨진 폰트명 192곳**: `'Lisu Bosa:Bold'` 같은 `패밀리:굵기` 표기 → 실제 패밀리명 + `font-weight` 숫자
2. **S-Core Dream CDN 사망**: `webfontworld/scdream` 404 → `style.css` 최상단에
   `projectnoonnu/noonfonts_six@1.2` 로 `@font-face` 6종(300·400·500·700·800·900) 직접 정의

**쓰는 패밀리는 딱 4개**: `Lisu Bosa` / `Pretendard Variable` / `S-Core Dream` / `Noto Sans KR`
→ CSS 쓸 때 `패밀리:굵기` 표기 절대 쓰지 말 것.

### 3-3. GNB / LNB

- `.gmb` 를 `position: fixed; top:0; left:0; z-index:1000` 으로 상단 고정
- 배경 `rgba(255,255,255,.7)`, 블러 `7px`
- 로고는 **`logo_RED.svg`** 사용 (중복 로고 제거됨)
- **검색 아이콘은 `STORE` 옆 기본 위치에 고정** (확장형 아님)
  - 이전 확장형 검색의 `.search.is-open`, `.search-input`, `data-search-*` CSS/JS 가 남아 있을 수 있음.
    검색을 다시 확장형으로 바꾸려면 현재 HTML 과 함께 정리 필요
- LNB 4개 패널 (`.gnb-lnb-panel[data-lnb="product|content|brand|store"]`)

| 메뉴 | 들여쓰기 | 컬럼 |
| --- | --- | --- |
| PRODUCT | 8px | 전체보기 · 제품 유형별(7) · 제품 라인별(2) · 기획전(3) · new · best |
| CONTENT | 100px | 메이크업 룩 · 퍼스널 컬러 · 소셜 미디어(2) · 소식&이벤트(2) · 멤버십(2) · 서포터즈(1) |
| BRAND | 185px | CONTACT(3) · About ETUDE |
| STORE | 261px | 매장(3) |

- 공통: 컬럼 간격 40px, 헤더→목록 20px, 항목 상하 4px, 색 `#3b3638`
- **호버·클릭 둘 다 동작** — 호버는 임시 펼침, 클릭은 고정(핀). 바깥 클릭/ESC 로 닫힘

### 3-4. 매장찾기 (Offline Etude)

- 지도 컨테이너 `top:245 left:686 921×610`, 지도는 원본 `Offline_map_full.png` 를 `1207.5×1152` 로 배치
- 렌즈 밖 블러 `blur(8px)`, 렌즈 클립 `circle(208px at 448.9px 301.53px)`
- 지점 박스: 배경 `rgba(250,250,250,.8)` + 패딩 `10px 20px`, `top:557`
- 안내 문구 `.offline-note`, 본문 색 `#333`
- 매장별 지도 좌표 (세 매장 모두 화면 y=368 정렬):

| 매장 | mapShift | mapShiftY |
| --- | --- | --- |
| 강남지상점 | 72 | 179 |
| 역삼점(기본) | 0 | 0 |
| 압구정점 | -200 | 146 |

- `MAP_LIMIT` 으로 이동 범위 제한

### 3-5. Event

- 모델 배경 투명, `ev_img.png`(609×827 원본 그대로 하단 정렬), 컨테이너 `609×876`
- 우측 컬럼 폭 `926`, 헤더 줄 `.event-head-row` + `더보기` 버튼
- `Event` 색 `#3b3638`, `Brand Story` 는 `transparent`

### 3-6. Instagram — 원통(cylinder) 캐러셀 + 영상

**영상 적용 완료**: `<video class="insta-card-media">` 7개, `Insta_Lilus_01~07.mp4` 사용.
`main.js` 의 `setPlaying()` 이 가운데 카드는 자동재생, 옆 카드는 호버 시 재생합니다.

카드를 **원통 위의 좌표**에 올려서 옆으로 갈수록 안쪽으로 말려 들어가고 뒤로 물러납니다.

```js
var RADIUS   = 1100;  // 원통 반지름 — 클수록 완만하게 휘고 좌우로 더 넓게 퍼짐
var STEP_DEG = 20;    // 카드 한 칸당 각도 — 클수록 급하게 휨

var ang = off * STEP_DEG * Math.PI / 180;   // off 는 부호 있고 드래그 중엔 소수
x    = RADIUS * Math.sin(ang);
z    = RADIUS * (Math.cos(ang) - 1);
rotY = -off * STEP_DEG;
```

| 위치 | 가운데 | ±1 | ±2 | ±3 |
| --- | --- | --- | --- | --- |
| x | 0 | ±376 | ±707 | ±953 |
| z (깊이) | 0 | −66 | −257 | −550 |
| 회전 | 0° | ∓20° | ∓40° | ∓60° |
| 배율 | 1.2 | 1.0 | 1.0 | 1.0 |

- 드래그 중에도 각도가 소수로 계산돼 링 전체가 손가락을 따라 매끄럽게 돌아갑니다
- **태블릿(≤1024px)은 평평한 일렬 배치** (`tabletInsta` 분기, `GAP_NEAR 334 / GAP_FAR 306`)
- 조절은 `RADIUS` / `STEP_DEG` 두 숫자만
- SNS 아이콘: 단색 SVG 를 CSS `mask` 로 찍어 색 제어
  - Instagram `#E4E4E4` → 호버 `#FF6AA1` → 클릭 `#F91F6F` / Footer `#575053` → 호버 `#F91F6F`

### 3-7. SINCE

- 배경 `Since.png` + 그라데이션, 곡선 `line.svg`(1572×1889 @ 117,119)
- 제품컷 6장 `since01~06` (03·05 는 회전 적용)
- 연도 라벨 6개: 2006 · 2007 · 2010 · 2015 · 2017 · 2021
  - 연결선 `#FFD3E3` 4×69px (`.since-conn`), 2006/2015 는 왼쪽·나머지는 오른쪽

### 3-8. Makeup Look

- 큰 카드 타이틀 배경: 하단 300px 그라데이션 스크림 (`.main_look_card_00::after`)
- **미니 상품 상자** (가로 100% 에서 깨졌던 것 수정됨):
  - 닫힌 상태 `336 × 62px` — **상품 1개만** 표시
  - 열린 상태 `336 × 159px` — 상품 3개를 **세로로** 표시
  - 메인 카드 `623 × 810px`
  - ⚠️ 상품 2개 이상을 가로로 배치했던 오류를 다시 만들지 말 것
- 상품 3종:
  1. `마이 쁘띠 팔레트 6.9g` / `#04 넌 나만 바라밤` → `makeup-mini-palette.png`
  2. `픽싱 틴트 10g` / `#01 너가젤루조아` → `makeup-mini-tint.png`
  3. `그림자 쉐딩 10g` / `#01 재창조` → `makeup-mini-shading.png`
- 세로 카드 6장: 호버 시 6px 상승 + 그림자 + `더보기 +`

### 3-9. Footer

- 로고 `logo_black.svg`(539×163 @ 314,347), SNS 검정 아이콘 4개(mask 방식), `top:9926px`

### 3-10. 섹션 순서 (데스크톱)

```
매장찾기     4325 ~  5405
이벤트       5405 ~  6485
Instagram   6498 ~  7660   ← Since 앞
SINCE       7783 ~  9926
Footer      9926 ~ 10427
.page min-height: 10427px  (피그마 원본과 동일)
```

### 3-11. GitHub Pages

`.github/workflows/deploy-pages.yml` 이 `main` 푸시마다 `static/` 을 아티팩트로 올립니다.

> ⚠️ 저장소 **Settings → Pages → Source** 가 `GitHub Actions` 여야 합니다.
> `Deploy from a branch` 로 두면 루트에 `index.html` 이 없어서 Jekyll 이 `README.md` 를
> 대신 렌더링합니다 (2026-07-31 에 실제로 이 상태였습니다).

### 3-12. Next.js 버전 제거 — 2026-07-31

`src/` 의 Next.js(App Router) 버전을 삭제했습니다. `static/` 과 8커밋 벌어져 있었고,
이미지가 담길 `public/` 이 `.gitignore` 에 걸려 있어 실행해도 이미지가 전부 깨지는 상태였습니다.

같이 지운 것: `package.json`, `package-lock.json`, `next.config.ts`, `tsconfig.json`,
`postcss.config.mjs`, `default_shadcn_theme.css`, `node_modules/`, `.next/`

되살리려면 `git show nextjs-last:src/...` (태그 = `62c67bf`)

---

## 4. 남은 일 / 확인 필요

0. 🔴 **GitHub Pages Source 설정이 아직 안 바뀌었습니다.** (2026-07-31 기준 미해결)
   - 현재: `Deploy from a branch` → 루트 URL 에 **README 가 렌더링**됩니다
   - 필요: Settings → Pages → Source 를 **`GitHub Actions`** 로 변경
   - **저장소 소유자만 할 수 있습니다.** 코드로는 못 고칩니다
   - 그때까지 볼 수 있는 주소: `https://manimani0317-crypto.github.io/etudehaeun/static/`
1. **Instagram 휘는 정도 확인** — 스케치와 비교해 `RADIUS`(1100) / `STEP_DEG`(20) 미세조정
   - 더 급하게 말리게 → `STEP_DEG` 25~28 / 더 넓게 → `RADIUS` 1300~1500
2. **`Insta_Lilus_08.mp4` 미사용** — 카드가 7장이라 8번 영상은 안 쓰이고 있습니다
3. **미사용 CSS 잔존** (렌더링에는 무해)
   `.메인페이지-div-2`, `.메인페이지-div-3`, `.since_box`, `.gray_line`, `.red_line`,
   `.chatgpt-image-…`, `.에뛰드로고_벡터변환-4`, `.아이디-2`, 확장형 검색 잔여 CSS
4. **`.claude/launch.json` 은 이 PC(EZEN) 전용 경로**입니다. 다른 PC 에서는 고쳐야 합니다
5. **`backup/local-pm0956-0729` 브랜치** — 07-29 로컬 작업 보관용. 불필요하면 삭제 가능
6. **눈으로 확인 권장**: LNB 4개 패널 정렬 / SINCE 제품컷 6장 위치 / Instagram 카드가 화면 밖으로 자연스럽게 흘러나가는지
7. **레이아웃 수정 후에는 최소 `1920px`, `1112px`, `768px` 에서 확인**

---

## 5. 검증 방법

### 브라우저

```js
// 폭이 꽉 찼는지 + 최상위 섹션 정렬
(() => {
  const page = document.querySelector('.page');
  return {
    zoom: getComputedStyle(page).zoom,
    renderedWidth: Math.round(page.getBoundingClientRect().width),
    hScroll: document.documentElement.scrollWidth - document.documentElement.clientWidth,
    allAtZero: ['.순정','.이벤트','.메인-footer','.메이크업','.new','.매장찾기','.메인슬라이더','.gmb']
      .every(s => Math.round(document.querySelector(s).getBoundingClientRect().left) === 0),
  };
})()

// Instagram 카드 좌표
[...document.querySelectorAll('[data-insta-card]')].map(c => c.style.transform)

// 폰트 적용 / 깨진 폰트명(0 이어야 정상) / 깨진 이미지
document.fonts.check('700 60px "Lisu Bosa"')
[...new Set([...document.querySelectorAll('*')].map(e=>getComputedStyle(e).fontFamily).filter(f=>f.includes(':')))].length
[...document.querySelectorAll('img')].filter(i=>i.complete && i.naturalWidth===0).map(i=>i.src)
```

### 정적 검증 (브라우저 막혔을 때)

- `src="images/..."` 경로가 실제로 존재하는지
- 새로 쓴 클래스가 CSS 에 정의돼 있는지
- `<div>` 태그 균형 / CSS 중괄호 균형 (`awk '{o+=gsub(/{/,"{"); c+=gsub(/}/,"}")} END{print o-c}'` → 0)

---

## 6. 참고 — 피그마 노드 ID

파일: `oN5K5v6JyNKEB8Qp44Bfs7` (김하은)

| 영역 | node-id |
| --- | --- |
| 메인페이지 전체 | `8406:1563` (1920 × 10427) |
| 태블릿 전체 | `8580:4925` (768 × 9841) |
| 매장찾기 | `8406:1687` |
| 이벤트 | `8406:1755` |
| SINCE | `8472:4115` |
| Instagram | `8578:3513` |
| Footer | `8406:1596` |
| Makeup Look | `8406:1672` |
| Makeup 미니 상품 | `8524:3864` |
| GNB 검색 | `8571:2789` |
| GNB+LNB PRODUCT / CONTENT / BRAND / STORE | `8570:2342` / `8570:2466` / `8570:2552` / `8570:2638` |

URL 형식: `https://www.figma.com/design/oN5K5v6JyNKEB8Qp44Bfs7/김하은?node-id=8406-1687`

---

## 7. 환경 메모

- Windows 10 / PowerShell 5.1 (`&&`, `||`, 삼항연산자 **사용 불가**)
- **node 없음 / python 은 Windows Store 스텁이라 동작 안 함 / ffmpeg 없음**
  → 스크립트는 **PowerShell** 로 작성해야 합니다
- `jq`, `bc` 없음
- **`gh` (GitHub CLI) 없음** → 저장소 설정 조회·변경, PR 작업 전부 불가.
  GitHub 상태를 확인해야 하면 웹으로 보거나 사용자에게 부탁해야 합니다
- **이 저장소는 npm 프로젝트가 아닙니다.** `npm run …` 은 전부 실패합니다
- heredoc 안에서 정규식 백슬래시가 깨질 때가 있음 → 복잡한 스크립트는 파일로 써서 실행
- 한글이 든 커밋 메시지는 **파일로 써서 `git commit -F <파일>`** 로 넣는 게 안전합니다
  (PowerShell 콘솔 출력이 한글을 깨뜨려 보여줄 때가 있는데, 파일 내용 자체는 멀쩡합니다)
- `.claude/settings.local.json` 에 `"defaultMode": "auto"` 설정됨

---

## 8. 변경 이력

### 2026-07-31 — 저장소 단순화 (`a3bf67e`, `0bf4f3a`)

**발단**: "GitHub Pages 링크를 열면 메인페이지가 아니라 명세서 같은 문서가 뜬다"

**진단** — 원인은 파일 위치가 아니라 **Pages 설정**이었습니다.

```
Pages Source = "Deploy from a branch (main / root)"
  → 저장소 루트에 index.html 이 없음  (실물은 static/index.html)
  → Jekyll 이 대신 README.md 를 렌더링
  → 그게 "명세서처럼 보이는 문서"의 정체
```

근거: 루트 URL 은 README, `/static/` 은 실제 사이트를 서빙하고 있었습니다.

**한 일**

1. **문서 정리** — README 2개가 현재 코드와 어긋나 있어 수정
   - `static/README.md`: 이미지 개수(39→88), `style.css` 구역 구성, `main.js` 범위
     (매장찾기만 → GNB/LNB·Instagram 포함), 반응형 설명(고정폭 → `zoom` 스케일링)
   - 삭제한 것: "Next.js 버전과 픽셀 단위로 완전히 일치 / 문서 높이 9926px / 이미지 50개"
     → 전부 07-30 오전 기준이라 셋 다 틀린 내용이었습니다 (실제 캔버스는 10427px)
   - 루트 `README.md`: `static/` 단일 기준으로 재작성, 배포 절 신설
2. **Next.js 버전 제거** — 상세는 3-12 절
3. **빈 `guidelines/` 제거** — Figma Make 기본 템플릿이 한 글자도 안 채워진 상태였음
4. 설정 정리 — `launch.json` 의 죽은 `etude-dev`, `settings.json` 의 npm 권한, `.gitignore` 의 Next.js 항목

**검증** — 로컬(3100) + 배포본 양쪽에서 확인

| 항목 | 결과 |
| --- | --- |
| 콘솔 에러 | 0 |
| 이미지 | 50개 로드, 깨진 것 0 |
| 영상 | 7개 |
| LNB 패널 | 4개 |
| 가로 스크롤 | 0 |
| 캔버스 높이 | 10427px 유지 |

**미해결** — Pages Source 설정 (4번 0항 참고). 저장소 소유자만 바꿀 수 있습니다.

**이 세션에서 얻은 교훈**

- 웹페이지 내용을 요약 도구로 확인했더니 **틀린 답**이 왔습니다
  ("루트에 사이트가 뜬다"고 했지만 실제로는 README 였음).
  GitHub 페이지나 배포 결과는 **브라우저로 직접 열어 DOM 을 확인**하는 게 확실합니다.
- PowerShell `Measure-Object -Line` 은 **빈 줄을 세지 않습니다.** 줄 수를 셀 땐
  `(Get-Content $p).Count` 를 쓰세요. (문서의 옛 줄 수가 이래서 어긋나 있었습니다)
