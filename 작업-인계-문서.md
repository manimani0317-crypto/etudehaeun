# ETUDE 메인페이지 — 작업 인계 문서

> 2026-07-30 작업분. 다른 곳에서 이 문서만 읽고 작업을 이어갈 수 있도록 정리했습니다.
> **새 대화를 시작할 때 이 파일을 먼저 읽어달라고 하면 됩니다.**

---

## 0. 이 프로젝트를 다룰 때의 기본 규칙

- **작업 대상은 `static/` 폴더뿐입니다.** 순수 HTML / CSS / JS 이고 프레임워크를 쓰지 않습니다.
  - `static/index.html` (22줄이지만 한 줄이 매우 김 — 통째로 읽지 말고 `node -e` 로 부분 추출할 것)
  - `static/css/style.css` (11,629줄)
  - `static/js/main.js` (457줄)
- `src/`, `public/`, `next.config.ts` 등 Next.js 쪽은 **건드리지 않습니다.** (`public/images/` 는 `.gitignore` 처리됨)
- `assets/` 는 디자이너가 넣어주는 원본 폴더. 쓸 이미지는 `static/images/main/` 으로 복사해서 씁니다.
- 설명은 한국어로. 주석도 한국어로 답니다.
- 디자인은 계속 수정되는 중이라, 손대기 전에 **현재 코드 상태부터 확인**하고 시작합니다.

### 미리보기 실행

`.claude/launch.json` 에 `etude-static` 설정이 있습니다 (포트 3100, 스크래치패드의 간단한 node 정적 서버).
Next.js 용 `etude-dev`(3000) 는 이 작업과 무관합니다.

```bash
node <스크래치패드>/static-server.js "C:/Users/haeun/Desktop/이젠 아카데미하은/etudehaeun/static" 3100
```

> 스크래치패드가 날아갔으면 `static/` 을 서빙하는 아무 정적 서버나 띄우면 됩니다.

---

## 1. 반드시 알아야 할 함정 (이번에 겪은 것들)

이걸 모르면 헛수고합니다.

### ① `[data-name="매장찾기"]` 는 **숨겨져 있습니다**
`style.css` 에 `[data-name="매장찾기"] { visibility: hidden !important; }` 가 있습니다.
피그마 export 원본 블록이고, **실제로 보이는 건 `[data-offline-section]`(`.div-20`) 인터랙티브 버전**입니다.
매장찾기를 고칠 땐 `.div-20 ~ .div-56` 쪽을 고쳐야 합니다.

### ② `.gmb` 안에 엉뚱한 게 들어있었습니다
export 구조상 `.gmb`(GNB) 블록 안에 Since 섹션 조각들이 자식으로 들어가 있었습니다.
지금은 정리해서 `.gmb > .gmb-inner > (.gmb-div, .gnb-lnb)` 만 남았습니다.
**DOM 을 추가할 땐 항상 부모가 어디인지 먼저 확인하세요.** (아래 3번 도구 참고)

### ③ 피그마 export 이미지는 **이미 잘린 결과물**입니다
`get_design_context` 가 주는 중첩 래퍼 + 퍼센트 좌표를 그대로 믿고 늘리면 **이미지가 찌그러집니다.**
- 예: `Offline_map.png`(921×610) = 지도 원본 `맵.png`(1284×1225) 를 디자인 기본 위치로 잘라낸 결과물
- 드래그가 필요한 지도는 **원본** `Offline_map_full.png`(=`맵.png`) 를 써야 함
- 반대로 `ev_img.png`(609×827) 는 잘린 결과물을 1:1 로 쓰는 게 정답

**판별법**: export 자산의 실측 크기가 컨테이너 크기와 똑같으면 "이미 잘린 것"입니다.

### ④ 피그마 MCP 는 간헐적으로 실패합니다
`get_design_context` / `get_metadata` 가 `Invalid JSON: EOF while parsing a string` 로 죽을 때가 있습니다.
**한 번 실패해도 재시도하면 됩니다.** (`get_screenshot` 은 거의 항상 성공)
`get_metadata` 는 이 파일에서 계속 실패했으니 기대하지 마세요.

### ⑤ 브라우저 도구가 자주 막힙니다
`classifier temporarily unavailable` 로 미리보기 조작이 안 될 때가 있습니다.
그럴 땐 아래 정적 검증 스크립트로 대신 확인하고, 나중에 다시 시도하세요.

---

## 2. 완료된 작업

### 2-1. 폰트 (전역 — 가장 임팩트 큰 수정)
페이지 전체 폰트가 안 먹고 있었습니다. 원인 두 가지:

1. **깨진 폰트명 192곳**: CSS 가 `'Lisu Bosa:Bold'`, `'S-Core Dream:9 Black'` 같이 존재하지 않는 이름을 사용
   (피그마 export 의 `패밀리:굵기` 표기) → 전부 실제 패밀리명 + `font-weight` 숫자로 변환
2. **S-Core Dream CDN 사망**: `webfontworld/scdream` 이 404 → `style.css` 최상단에 살아있는 CDN
   (`projectnoonnu/noonfonts_six@1.2`) 으로 `@font-face` 6종(300·400·500·700·800·900) 직접 정의.
   `index.html` 의 죽은 링크는 제거함

**현재 쓰는 패밀리는 딱 4개**: `Lisu Bosa` / `Pretendard Variable` / `S-Core Dream` / `Noto Sans KR`
→ 앞으로 CSS 쓸 때 `패밀리:굵기` 표기 절대 쓰지 말 것.

### 2-2. GNB / LNB (신규)
- `.gmb` 를 `position: fixed; top:0; left:0; z-index:1000` 으로 **상단 고정**
- 배경 `rgba(255,255,255,.7)`, 블러 `7px`, 로고 `111×34` (디자인값)
- LNB 4개 패널 신규 (`.gnb-lnb-panel[data-lnb="product|content|brand|store"]`)

| 메뉴 | 들여쓰기 | 컬럼 |
|---|---|---|
| PRODUCT | 8px | 전체보기 · 제품 유형별(7) · 제품 라인별(2) · 기획전(3) · new · best |
| CONTENT | 100px | 메이크업 룩 · 퍼스널 컬러 · 소셜 미디어(2) · 소식&이벤트(2) · 멤버십(2) · 서포터즈(1) |
| BRAND | 185px | CONTACT(3) · About ETUDE |
| STORE | 261px | 매장(3) |

- 공통: 컬럼 간격 40px, 헤더→목록 20px, 항목 상하 4px, 색 `#3b3638`
- **호버·클릭 둘 다 동작** — 호버는 임시로 펼침, 클릭은 고정(핀). 바깥 클릭/ESC 로 닫힘
- 열린 메뉴에 핑크 밑줄이 따라감

### 2-3. 매장찾기 (Offline Etude)
인터랙션은 그대로 두고 디자인만 갱신:
- 지도 컨테이너 `top:245 left:686 921×610`, 지도는 원본 `Offline_map_full.png` 를 `1207.5×1152` 로 배치
- 렌즈 밖 블러 `blur(8px)` (기존 `blur(4px) brightness(.82)` → 어둡게 대신 흐리게)
- 렌즈 클립 `circle(208px at 448.9px 301.53px)`
- 지점 박스 강조: 배경 `rgba(250,250,250,.8)` + 패딩 `10px 20px`, `top:557`
- 안내 문구 신규: `*표기된 3곳은 예시이며 …` (`.offline-note`)
- 본문 색 `#737373 → #333`
- **매장별 지도 좌표 재계산** (세 매장 모두 화면 y=368 로 정렬):

| 매장 | mapShift | mapShiftY |
|---|---|---|
| 강남지상점 | 72 | 179 |
| 역삼점(기본) | 0 | 0 |
| 압구정점 | -200 | 146 |

- `MAP_LIMIT` 으로 이동 범위를 제한해 가장자리가 비지 않게 함

### 2-4. Event
- 모델 배경 제거(투명), 이미지 `event_model.png → ev_img.png` (609×827 원본 그대로 하단 정렬)
- `Event_Model` 컨테이너 `609×876`
- 우측 컬럼 폭 `818 → 926`
- 헤더 줄 신규(`.event-head-row`) + `더보기` 버튼 신규
- `Event` 색 `#3b3638`, `Brand Story` 는 `transparent` (디자인값)

### 2-5. Instagram (전체 교체 + 3D 링 캐러셀)
기존 마크업 삭제 후 새로 작성. 배경 **흰색**.
- 카드 7장, `perspective:1800px` 위에서 `rotateY` 로 **양 끝이 사다리꼴**로 눕는 링

| 위치 | 가운데 | ±1 | ±2 | ±3 |
|---|---|---|---|---|
| 각도 | 0° | ∓32° | ∓42° | ∓42° |
| 배율 | 1.2 | 1.0 | 0.91 | 0.82 |
| x(px) | 0 | ±230 | ±480 | ±730 |

- 드래그하면 손가락을 실시간 추종(소수점 위치) → 놓으면 가장 가까운 칸으로 안착
- 가운데는 항상 크게 + 자동재생, 호버하면 재생 + 아이템 박스 상승, 클릭하면 가운데로
- SNS 아이콘: 단색 SVG 를 CSS `mask` 로 찍어 색 제어 (`<img>` → `<button>`)
  - Instagram `#E4E4E4` → 호버 `#FF6AA1` → 클릭 `#F91F6F`
  - Footer `#575053` → 호버 `#F91F6F`

> **⚠️ 영상 미적용**: 에셋에 영상 파일이 없어 `Component_02~14.png` 7장으로 대체했습니다.
> 영상이 생기면 `<img class="insta-card-media">` 를 아래로 바꾸기만 하면 재생/정지가 그대로 붙습니다.
> ```html
> <video class="insta-card-media" src="..." muted loop playsinline poster="..."></video>
> ```
> `main.js` 의 `setPlaying()` 이 `play()` 가 있는 요소만 처리하도록 되어 있습니다.

### 2-6. SINCE (전체 교체)
기존 마크업 + 옛 배경(`chatgpt-image…`) 삭제 후 새로 작성.
- 배경 `Since.png` + 그라데이션, 곡선 `line.svg`(1572×1889 @ 117,119)
- 제품컷 6장 `since01~06` (디자인 좌표 그대로, 03·05 는 회전 적용)
- 연도 라벨 6개: 2006 · 2007 · 2010 · 2015 · 2017 · 2021
  - 연결선은 `#FFD3E3` 4×69px (`.since-conn`), 2006/2015 는 선이 왼쪽·나머지는 오른쪽

### 2-7. Makeup Look
- **큰 카드 타이틀 배경**: 하단 300px 그라데이션 스크림 (`.main_look_card_00::after`)
- **아이템 박스 미니 슬라이더**: 아래 화살표 클릭 → 창이 44px 내려오고 화살표 뒤집힘 +
  인디케이터 등장, 아이템 3개를 점 클릭·드래그로 이동
  - 아이템은 페이지에 있는 실제 제품 사용: 마이 쁘띠 팔레트 6.9g / 럭키 립햄치크 1.5g / 픽싱 틴트 4g
- **세로 카드 6장**: 호버 시 6px 상승 + 그림자 + `더보기 +` 등장

### 2-8. Footer
- 로고를 `logo_black.svg`(539×163 @ 314,347) 로 교체
- SNS 를 검정 아이콘 4개(mask 방식)로 교체
- 위치 `bottom:0` → `top:9926px`

### 2-9. 섹션 순서 바로잡음
디자인과 순서가 달랐습니다(원래는 SINCE 가 Instagram 보다 위).

```
매장찾기     4325 ~  5405
이벤트       5405 ~  6485
Instagram   6550 ~  7721   ← Since 앞으로 이동
SINCE       7783 ~  9926
Footer      9926 ~ 10427
.page min-height: 10427px  (피그마 원본과 동일)
```

푸터가 마지막 섹션을 덮던 문제도 함께 해결됨.

---

## 3. 검증 방법

### 브라우저 (가능할 때)
```js
// 폰트가 실제로 적용됐는지
document.fonts.check('700 60px "Lisu Bosa"')
// 깨진 폰트명이 남았는지 (0 이어야 정상)
[...new Set([...document.querySelectorAll('*')].map(e=>getComputedStyle(e).fontFamily).filter(f=>f.includes(':')))].length
// 깨진 이미지
[...document.querySelectorAll('img')].filter(i=>i.complete && i.naturalWidth===0).map(i=>i.src)
```

섹션이 화면 캡처에서 너무 작게 나올 땐, 해당 섹션을 `cloneNode` 해서
`position:fixed` 오버레이에 `transform: scale()` 로 확대한 뒤 캡처하면 잘 보입니다.

### 정적 검증 (브라우저 막혔을 때)
`node` 로 아래를 확인하면 대부분 잡힙니다.
- `src="images/..."` 경로가 실제로 존재하는지
- 새로 쓴 클래스가 CSS 에 정의돼 있는지
- `<div>` 태그 균형 (열림/닫힘 카운트가 0)
- CSS 중괄호 균형
- `node --check static/js/main.js`

### DOM 구조 파악용 스니펫
```js
function blockEnd(str, st) {                 // 특정 요소가 끝나는 위치
  const re = /<div\b[^>]*>|<\/div>/g; re.lastIndex = st;
  let d = 0, m;
  while ((m = re.exec(str))) { if (m[0]==='</div>') { d--; if (!d) return m.index+6; } else d++; }
  return -1;
}
```
자식 목록을 뽑을 땐 같은 방식으로 깊이 2 인 태그만 모으면 됩니다.

---

## 4. 남은 일 / 확인 필요

1. **Instagram 영상 교체** — 위 2-5 참고. 파일만 받으면 바로 적용 가능
2. **미사용 CSS 잔존** — 삭제된 옛 마크업의 규칙이 남아 있음 (렌더링에는 무해)
   `.메인페이지-div-2`, `.메인페이지-div-3`, `.since_box`, `.gray_line`, `.red_line`,
   `.chatgpt-image-…`, `.에뛰드로고_벡터변환-4`, `.아이디-2`
3. **눈으로 확인 권장**
   - LNB 4개 패널의 좌우 정렬(각 대메뉴 바로 아래에 붙는지)
   - SINCE 제품컷 6장이 곡선 위 제자리에 앉는지
   - Instagram 카드가 화면 밖으로 자연스럽게 흘러나가는지
4. **커밋 안 됨** — 현재 251개 파일이 staged/변경 상태. 마지막 커밋은 `f29d475 0729`

---

## 5. 참고 — 피그마 노드 ID

파일: `oN5K5v6JyNKEB8Qp44Bfs7` (김하은)

| 영역 | node-id |
|---|---|
| 메인페이지 전체 | `8406:1563` |
| 매장찾기 | `8406:1687` |
| 이벤트 | `8406:1755` |
| SINCE | `8472:4115` |
| Instagram | `8578:3513` |
| Footer | `8406:1596` |
| GNB+LNB PRODUCT | `8570:2342` |
| GNB+LNB CONTENT | `8570:2466` |
| GNB+LNB BRAND | `8570:2552` |
| GNB+LNB STORE | `8570:2638` |

URL 형식: `https://www.figma.com/design/oN5K5v6JyNKEB8Qp44Bfs7/김하은?node-id=8406-1687`

---

## 6. 환경 메모

- Windows 10 / PowerShell + Git Bash 둘 다 사용 가능
- `python` 은 Windows Store 스텁이라 **동작하지 않음** → 스크립트는 `node -e` 로 작성할 것
- `jq`, `bc` 없음
- heredoc(`<< 'EOF'`) 안에서 정규식 백슬래시가 깨질 때가 있음 → 복잡한 스크립트는
  스크래치패드에 파일로 쓴 뒤 `node <파일>` 로 실행하는 게 안전
- `.claude/settings.local.json` 에 `"defaultMode": "auto"` 설정됨 (권한 확인창 최소화, 다음 세션부터 적용)
